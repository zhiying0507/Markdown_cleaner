# 企业级技术 Markdown 清洗规则 v1.0-pilot

## 目标

从 MinerU 生成的 Markdown 中删除版面和解析噪声，同时保护技术正文、数值、单位、公式、代码、参数表和可追溯信息。清洗结果不得覆盖源文件。

## 数据分层

- `raw`：原始 MinerU Markdown，只读。
- `cleaned`：保真清洗后的 Markdown。
- `reports`：每个文件的清洗前后统计、规则动作和质量状态。
- `artifacts/*.tables.jsonl`：结构化表格记录。
- `artifacts/*.images.jsonl`：被处理图片的路径、图注和原始行号。
- `artifacts/*.quarantined_tables.jsonl`：疑似解析损坏的表格及转换结果。

## 确定性清洗规则

1. 统一 UTF-8 和 LF，删除 BOM、软连字符、零宽字符。
2. 仅在识别到安全起止标题时删除前置目录。
3. 仅在文档后 25% 区域删除独立 `Index` 章节。
4. 图片处理由配置决定；试点采用删除图片及相邻 `Figure` 图注，同时写入图片审计清单。
5. HTML 表格使用 HTML 解析器转换，禁止使用正则拆单元格。
6. 表格保留原始行序和重复键；不生成“可能是别名”等推断性文本。
7. Markdown 单元格中的竖线转义为 `\|`，合并单元格展开为矩形网格。
8. 超长单元格、列数异常或疑似 MinerU 单元格挤压的表格进入隔离队列。
9. 编号标题按编号深度修复；相邻 `Chapter N` 与章节名合并；仅将前后为空行、包含文字且不以句号结尾的孤立编号短行补为标题。
10. 代码内容不改写。试点删除 MinerU 低可信代码语言标签；若一个围栏错误吞入了独立编号章节标题，则只移除该块的外层围栏，保留全部正文和缩进内容并记录动作。
11. 不自动跨行去连字符，不自动纠正 OCR 数字，不改写 LaTeX 公式。
12. Unicode 项目符号仅在代码块外转换成 Markdown `-`。

## 质量门禁

- 输出必须仍为合法 UTF-8。
- 代码围栏必须成对闭合。
- 不得残留 HTML `<table>` 和 Markdown 图片引用。
- 清洗规则必须幂等：对结果再次清洗，内容不能变化。
- 字符保留率默认必须处于 60%～110%。
- 疑似损坏表格使文档进入 `REVIEW`，不能静默进入全量正式数据集。
- 图被删除后仍存在的 Figure 引用计为警告，不自动删除相关正文。

## 状态定义

- `PASS`：通过全部质量门禁。
- `PASS_WARN`：结构合格，但存在悬空图引用等非致命警告。
- `REVIEW`：存在疑似损坏表格或异常字符保留率，需要抽查。
- `REJECT`：残留目标噪声、代码围栏损坏或非幂等，不得入库。

## 全量运行约束

两份 ngspice 样本验收后，本版本只能直接用于相同 MinerU 技术手册。PCB 书籍需要补充包含中文、复杂表格、原理图和公式的代表性样本，形成回归集后再启用全量目录。高价值技术图片建议改用 `keep_caption` 或 `placeholder` 策略。

批处理支持 `--resume`。只有源内容哈希、配置哈希、清洗版本以及全部输出产物均一致时才跳过；任何一项变化都会重新处理该文件。
